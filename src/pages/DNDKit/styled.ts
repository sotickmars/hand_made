import { DndContext as DndContextKit } from '@dnd-kit/core';
import styled from 'styled-components';

export const DndContext = styled.div`
  padding: 100px;
  overflow-y: scroll;
  background: red;
  height: 500px;
`;
